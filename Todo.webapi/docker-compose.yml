version: '3.4'

services:
  todo-app-api:
     image: todo-app-api
     build:
      context: C:\Users\ser-9\source\repos\TodoSln\Todo.webapi\
      dockerfile: Dockerfile
     ports:
     - "8980:80"
     networks:
     - mynetwork

networks:
  mynetwork:
    driver: bridge